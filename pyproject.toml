[build-system]
requires = ["setuptools>=46.1.0", "setuptools_scm[toml]>=5"]
build-backend = "setuptools.build_meta"

[project]
name = "snakestream"
description = "Java like streams for snakes"
readme = "README.md"
license = { file = "LICENSE.txt" }
authors = [
    { name = "Tommy Derngren", email = "tommy.derngren@hiq.se" },
]
requires-python = ">=3.14"
classifiers = [
    "Development Status :: 4 - Beta",
    "Programming Language :: Python",
]
dynamic = ["version"]

[project.urls]
Documentation = "https://github.com/carby/snakestream/wiki"

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["tests"]

[tool.setuptools_scm]
version_scheme = "no-guess-dev"

[dependency-groups]
dev = [
    "hypothesis",
    "pytest",
    "pytest-cov",
    "pytest-asyncio",
    "pytest-mock",
    "ruff",
    "ty",
]

[tool.pytest.ini_options]
addopts = "--cov snakestream --cov-report term-missing --verbose"
norecursedirs = ["dist", "build", ".tox", ".virtualenv"]
testpaths = ["tests"]

[tool.coverage.run]
# branch = true folds branch-arc coverage into the same "percent covered"
# figure as line coverage, so CI's --cov-fail-under=98 (see check.yml)
# already gates on combined line+branch coverage, not line coverage alone.
branch = true
source = ["snakestream"]

[tool.coverage.paths]
source = ["src/", "*/site-packages/"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self\\.debug",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]

[tool.ruff]
line-length = 127
target-version = "py314"

[tool.ruff.lint]
# Widened from E,F,W,C90,UP on 2026-08-26. The added families were trialled
# over src/ first: nine findings total, all either fixed in the same change or
# suppressed at their site with the reason. ASYNC found nothing, which is the
# point of enabling it -- it costs no cleanup and guards the one thing this
# library is entirely made of.
select = ["E", "F", "W", "C90", "UP", "ASYNC", "B", "SIM", "RUF", "PERF", "C4", "RET", "PIE", "FURB", "PLR", "PLW", "PT"]
mccabe = { max-complexity = 10 }

# One rule, not a family. PLR2004 flags every literal a test compares against
# -- `lambda x: x > 5`, `assert largest == 7` -- but in a test that literal is
# the data and the expected value, and naming it moves the interesting number
# off the interesting line. 218 of the 283 findings over tests/ were this one
# rule. It stays enforced over src/, where an unexplained constant is a real
# finding, and every other rule in PLR still applies to tests/.
[tool.ruff.lint.per-file-ignores]
"tests/**" = ["PLR2004"]
