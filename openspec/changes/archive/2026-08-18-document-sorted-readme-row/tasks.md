## 1. README

- [x] 1.1 Add a `sorted(comparator: Comparator | None = None, reverse: bool = False)` row to README's Stream API table (`x`-prefixed done section), alphabetically between `skip()` and `to_array()`, describing behavior matching `stream.py:191`.
