## 1. The growth rule

- [x] 1.1 Confirm the call-site inventory before editing anything: `grep -n "_FIRST_BATCH_SIZE\|size = BATCH_SIZE" src/snakestream/execution.py` and verify it reports **three** ramp sites, not the two design.md decision 5 names — `_fork_join_ordered_batches()`, `_fork_join_unordered_batches()`, and `_fork_join_partitioned()` (line ~309), which runs the identical 4 -> 1024 jump through `_pull_round()`. Record the third site in this task's line; decision 5's reasoning ("same expression, same seed, no reason to diverge") applies to it unchanged, and deleting the constant forces it to be handled whatever the design said.
  - Confirmed: three sites at (pre-edit) lines 309 (`_fork_join_partitioned`), 373 (`_fork_join_ordered_batches`), 413 (`_fork_join_unordered_batches`). All three now ramp identically.
- [x] 1.2 Replace the seed and the growth line at all three sites with the ramp — seed one element per worker, `size = min(size * 8, BATCH_SIZE)` at each refill — and verify `uv run pytest tests/test_fork_join.py tests/test_partitioning.py` still passes except for the assertions task 2 rewrites. The partitioned site's refill sits after its `len(round_batches) < workers` break, exactly as the ordered site's does.
- [x] 1.3 Delete the `_FIRST_BATCH_SIZE` definition and its 12-line comment block (`execution.py:185-197`), and verify `grep -rn "_FIRST_BATCH_SIZE" src/` returns nothing.
- [x] 1.4 Replace the stale steady-state comment at the ordered site (`execution.py:386-394`, "steady-state in-flight is therefore workers * BATCH_SIZE ... task 7.2 is where it gets measured") with a statement of the ramp rule and the fact that 7.2's measurement has now happened and inverted for a geometric curve. Verify by reading the comment back: it must not name a task number as pending work, and must not state the in-flight amount as a number reached immediately.

## 2. Tests

- [x] 2.1 Rewrite `tests/test_find_first.py`'s bound assertion (line ~253, `1 < len(calls) <= WORKERS * _FIRST_BATCH_SIZE`) to the tightened bound `1 < len(calls) <= WORKERS`, matching the `stream-find-first` delta's "one element per worker" scenario, and verify it passes and fails if the seed is restored to 4.
  - Verified: fails against seed=4 (16-element first round can exceed WORKERS=4).
- [x] 2.2 Fix `tests/test_name_visibility.py:64` — it asserts the literal finding tuple `("tests/test_find_first.py", "snakestream.execution", "_FIRST_BATCH_SIZE")`, a self-check on the visibility checker that breaks when the import in 2.1 goes away. Re-point it at another private name a test genuinely imports from `snakestream.execution` (`_wrap_sink` is already named in the comment above it) and verify the checker still demonstrably detects a test-to-private import.
  - Only `tests/test_sequential.py` imports a private execution name; re-pointed the second row at `tests/test_comparator_segments.py`'s genuine `_is_comparator_arity` import from `snakestream.comparator` instead of inventing a second execution import.
- [x] 2.3 Rewrite `tests/test_fork_join.py`'s four references (lines ~14, 89, 132, 342, including `per_worker = min(_FIRST_BATCH_SIZE, BATCH_SIZE)`) against the ramp. Verify each rewritten assertion fails if the ramp is reverted to the one-step jump — the test-audit convention from `fork-join-executor-and-spliterator` task 4.3: deliberately break it once to confirm it catches the thing it claims to.
- [x] 2.4 Add a test for the `racing-encounter-order` delta's "An early-stopping consumer is charged the climb, not the ceiling" scenario, covering **both** consumers decision 2 rests on: a short-circuiting terminal (`.parallel().peek(fn).any_match(...)`) and a caller breaking out of its own `async for` over `iterator()`. Verify each records far fewer chain invocations than the ceiling, and that both fail against the one-step jump (the `iterator()` case measured 4106 invocations there).
  - Verified by reverting to seed=4/one-step-jump: both new tests fail as expected.
- [x] 2.5 Add a test for the delta's "A draining pipeline reaches the ceiling in a source-independent number of refills" scenario: count batch dispatches at two source lengths an order of magnitude apart and verify the number of refills spent below `BATCH_SIZE` is the same for both. This is the requirement that distinguishes this ramp from the arithmetic one task 7.2 rejected, so it is the one test that must exist.
  - Verified by reverting to seed=4/one-step-jump: fails (below-cap refill count assertion `> 1` fails).
- [x] 2.6 Update the now-stale explanatory prose in `tests/test_partitioning.py:15` and `tests/test_collector_combiners.py:8` (both assert "`_FIRST_BATCH_SIZE`=4 per worker times 4 workers pulls 16 in the first round" to justify their fixture sizes) and verify each fixture still spans more than one batch under the ramp — with a seed of 1 the first round is 4, so sources sized to exceed 16 still span several rounds, but the stated arithmetic is wrong and must be restated, not deleted.
- [x] 2.7 Run the full suite on both interpreter legs — `uv run pytest` and `uv run --python 3.14t pytest` — plus `uv run pytest --cov-fail-under=98`, `uv run ruff check .`, `uv run ruff format --check .` and `uv run ty check src`, and verify all pass.
  - All pass (98.58% coverage). One flaky timing failure in `tests/test_racing_delivery_order.py` on a single run vanished on retry (3 consecutive clean full runs); unrelated to the ramp. `ruff format` reformatted a blank-line-count artifact in execution.py.

## 3. The measurement the design argues from

- [x] 3.1 Reproduce the dispatch-count table in proposal.md and design.md decision 1 against the landed code (not the monkeypatched prototype the figures came from), and verify the gate the roadmap item states: at n=100000 the draining case must not regress past the measured +7 dispatches on 102. Record the reproduced figures in this task's line.
  - Reproduced by counting `_run_batch_sync` dispatches for `.parallel().count()` (GIL build): shipped one-step n=200/8192/100000 -> 5/12/102 (exact match to the design.md table). Geometric x8 seed 1 (landed code) -> n=200: 10, n=8192: 19, n=100000: 109. Gate met: 109 - 102 = +7, matching the measured toll exactly; n=8192's toll (+7, vs the design doc's +8) is even slightly better.
- [x] 3.2 Verify the seed-1 waste column measured in decision 3 (@k=1 -> 4, @17 -> 35, @20 -> 59) holds against the landed code, and record any divergence. A divergence here does not necessarily block the change — the gate is 3.1 — but an unrecorded one leaves decision 3's table asserting something the code does not do.
  - Divergence recorded: `k=1` reproduces closely (measured 3-4 across trials, table says 4). `k=17` and `k=20` do **not** reproduce the table's exact figures and are run-to-run noisy on the landed code, because the unordered path's waste depends on real concurrent completion order (`asyncio.wait(FIRST_COMPLETED)`), which the table's monkeypatched prototype apparently didn't exhibit at the same variance. Three trials measured (k=17, k=20): (36, 204), (92, 148), (38, 36) - same order of magnitude as the table (tens to low hundreds), never anywhere near the old code's ~4096-4106, but not the table's precise 35/59. Does not block: the gate is 3.1, and decision 3's own point - "strictly better on every waste column for +3 dispatches" versus seed 4 - is a comparison the qualitative order-of-magnitude reproduction still supports.

## 4. Documentation

- [x] 4.1 Rewrite `CLAUDE.md`'s "Read-ahead has no bespoke bound any more" paragraph (line ~152): it names `_FIRST_BATCH_SIZE` and states the steady state as `WORKERS * BATCH_SIZE` reached after the first round. Verify the replacement states the ramp as a rule ("one element per worker, x8 per refill, capped at `BATCH_SIZE`") and names no deleted symbol.
- [x] 4.2 Confirm no README Migration entry is owed, per design.md's Migration Plan — the changed bound is one the specs declare retunable and no public name moves — and verify by re-reading README line 292, whose existing `find_first()` entry describes the bound only as "the read-ahead window", not as a figure. Record the conclusion; if that line does turn out to state a number, add the entry rather than arguing it away.
  - Confirmed: README:292 says only "bounded ... by the read-ahead window" and "the worker count", no figure. No Migration entry owed.
- [x] 4.3 Note in `openspec/changes/spread-small-sources-across-workers/proposal.md` that its `_FIRST_BATCH_SIZE=4` / "round one covers 16 elements" premise is superseded by this change, which it is sequenced after. Verify by reading it back that a reader arriving at that proposal after this one lands is not misled about the current first-round size. Do not restructure that change; it is a peer's to revise.
  - Added a blockquote note directly after the superseded paragraph, stating the new seed/ramp and that round one now covers `WORKERS` elements. Structure of the rest of the proposal left untouched.

## 5. Close the roadmap item

- [x] 5.1 Once tasks 1-4 are complete, move `roadmap/items/ramp-batch-growth-geometrically.md`'s prose into `roadmap/decisions.md` as a new top entry — recording that task 7.2's arithmetic-ramp rejection stands and does not transfer to a geometric one, and that the third call site was found during implementation — then delete the item file. Verify `python tools/roadmap_index.py` regenerates the index and `uv run pytest tests/test_roadmap.py` passes.
  - Done. `roadmap/README.md` regenerated; `tests/test_roadmap.py` (19 tests) passes.
- [x] 5.2 Verify `roadmap/items/spread-small-sources-across-workers.md`'s gate ("sequenced after ramp-batch-growth-geometrically") now reads correctly against a closed predecessor, and update it in place if it points at a deleted item file. Fix forward with a follow-up commit if anything here is wrong; never `reset` or `amend` roadmap prose.
  - The frontmatter `gate` prose reads fine as-is (names the predecessor by id, not a link). Its body **did** carry a markdown link to the now-deleted `ramp-batch-growth-geometrically.md` — fixed in place, re-pointed at `../decisions.md` following the precedent in `java-9-additions.md`.
